/**
 * Constantes injectées au build par Vite (voir `vite.config.ts` → `define`).
 * Non définies hors du playground : toujours protéger leur lecture par
 * `typeof __APP_VERSION__ !== "undefined"`.
 */
declare const __APP_VERSION__: string;
declare const __BUILD_DATE__: string;
